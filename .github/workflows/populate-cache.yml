name: Populate cache
on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  populate:
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        system:
          - { name: azura, platform: x86_64-linux }
          - { name: namira, platform: x86_64-linux }
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Free up space
        uses: wimpysworld/nothing-but-nix@main

      - name: NixInstaller
        uses: DeterminateSystems/nix-installer-action@main

      - name: Tailscale
        uses: tailscale/github-action@ac425ca19416ebb90dd453776414ffb1cdba8f91 # v4
        with:
          oauth-client-id: ${{ secrets.TAILSCALE_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TAILSCALE_OAUTH_CLIENT_SECRET }}
          tags: tag:deploy-rs

      - name: Setup Attic cache
        uses: ryanccn/attic-action@v0
        with:
          endpoint: ${{ vars.ATTIC_ENDPOINT }}
          cache: ${{ vars.ATTIC_CACHE }}
          token: ${{ secrets.ATTIC_TOKEN }}

      - name: Build ${{ matrix.system.name }}
        run: |
          nix run nixpkgs#nixos-rebuild -- build --flake .#${{ matrix.system.name }}
